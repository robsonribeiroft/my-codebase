/*
  Map a singly linked list,
  executing COMP,
  Given the reference of head
*/
void mapLinkedList(struct Node * node)
{
  while (node != NULL)
    {
      // COMP
      
      node = node->next;
    }
}
